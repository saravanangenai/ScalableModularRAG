# Spec: <title>

- **ID:** `<NNN-slug>`
- **Roadmap phase:** <link to specs/architecture/08-roadmap.md phase, or "ad-hoc">
- **Status:** draft
- **Owner:** <who requested / who's driving>
- **Date:** <YYYY-MM-DD>

## Problem statement

What's broken or missing today, in one or two paragraphs. Reference the current behavior
concretely (a file, a user-visible gap, a rubric item) rather than describing an abstract
ideal.

## Goals

- Bullet list of what this increment must achieve, stated as outcomes, not implementation.

## Non-goals

- Explicitly out of scope for this spec, so reviewers don't assume it's covered. Things
  deferred to a later phase belong here with a pointer to that phase.

## User-facing behavior

Describe what a user (end user, admin, or API consumer, as applicable) can now do that they
couldn't before. Include example requests/responses or UI flows at a black-box level — no
internal schema or library names yet.

## Acceptance criteria

Testable, falsifiable statements. Each should be checkable by running something or looking
at something specific.

- [ ] ...
- [ ] ...

## Constraints

Anything that limits the solution space: must not break X, must be backward compatible with
Y, must fit within existing auth model, performance/cost bounds, etc.

## Open questions

Anything genuinely undecided that needs the user's input before or during planning.
