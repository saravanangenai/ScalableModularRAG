{
  "info": {
    "name": "MM-RAG Auth (011-auth-and-workspaces)",
    "_postman_id": "mm-rag-auth-011",
    "description": "Exercises apps/api's auth + tenant/workspace/membership/api-key routes against real Keycloak + Postgres (no mocks). Run folders top to bottom on a fresh run: Auth -> Tenants -> Workspaces -> API Keys -> Negative/RBAC.\n\nSetup:\n1. Import the companion environment file (mm-rag-local.postman_environment.json) and select it.\n2. Fill in `client_secret` from infra/.env's KEYCLOAK_TEST_CLIENT_SECRET (left blank here deliberately — never commit a real secret into this file).\n3. Make sure apps/api is running (`uv run uvicorn apps.api.main:app --host 127.0.0.1 --port 8000`) and Keycloak is running (`http://localhost:8080`).\n\nKnown gap: there is no GET /me endpoint yet, so getting User 2's *internal* user id (needed for the 'Add Tenant Member' request) requires a direct DB lookup after User 2's first authenticated call (JIT-provisioning) has run:\n  psql -U mmrag -h localhost -d mmrag -c \"SELECT id FROM users WHERE email = 'testuser2@example.com';\"\nPaste that id into the `user2_internal_id` variable. See the 'Add Tenant Member (user2)' request description for the exact step.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    { "key": "base_url", "value": "http://127.0.0.1:8000", "type": "string" },
    { "key": "keycloak_url", "value": "http://localhost:8080", "type": "string" },
    { "key": "realm", "value": "mm-rag", "type": "string" },
    { "key": "client_id", "value": "mm-rag-api", "type": "string" },
    { "key": "client_secret", "value": "", "type": "string" },
    { "key": "user1_username", "value": "testuser", "type": "string" },
    { "key": "user1_password", "value": "testuser-changeme", "type": "string" },
    { "key": "user2_username", "value": "testuser2", "type": "string" },
    { "key": "user2_password", "value": "testuser2-changeme", "type": "string" },
    { "key": "access_token_1", "value": "", "type": "string" },
    { "key": "access_token_2", "value": "", "type": "string" },
    { "key": "user2_internal_id", "value": "", "type": "string" },
    { "key": "tenant_id", "value": "", "type": "string" },
    { "key": "workspace_id", "value": "", "type": "string" },
    { "key": "api_key_id", "value": "", "type": "string" },
    { "key": "api_key_plaintext", "value": "", "type": "string" }
  ],
  "item": [
    {
      "name": "0 - Health",
      "item": [
        {
          "name": "Health check (no auth)",
          "request": {
            "method": "GET",
            "header": [],
            "url": { "raw": "{{base_url}}/health", "host": ["{{base_url}}"], "path": ["health"] }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "pm.test('status 200', () => pm.response.to.have.status(200));",
                  "pm.test('body is ok', () => pm.expect(pm.response.json().status).to.eql('ok'));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "1 - Auth (Keycloak token minting)",
      "item": [
        {
          "name": "Get Token - User 1 (testuser)",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/x-www-form-urlencoded" }],
            "url": {
              "raw": "{{keycloak_url}}/realms/{{realm}}/protocol/openid-connect/token",
              "host": ["{{keycloak_url}}"],
              "path": ["realms", "{{realm}}", "protocol", "openid-connect", "token"]
            },
            "body": {
              "mode": "urlencoded",
              "urlencoded": [
                { "key": "grant_type", "value": "password" },
                { "key": "client_id", "value": "{{client_id}}" },
                { "key": "client_secret", "value": "{{client_secret}}" },
                { "key": "username", "value": "{{user1_username}}" },
                { "key": "password", "value": "{{user1_password}}" }
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "pm.test('status 200', () => pm.response.to.have.status(200));",
                  "const json = pm.response.json();",
                  "pm.collectionVariables.set('access_token_1', json.access_token);",
                  "pm.test('access_token present', () => pm.expect(json.access_token).to.be.a('string'));"
                ]
              }
            }
          ]
        },
        {
          "name": "Get Token - User 2 (testuser2)",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/x-www-form-urlencoded" }],
            "url": {
              "raw": "{{keycloak_url}}/realms/{{realm}}/protocol/openid-connect/token",
              "host": ["{{keycloak_url}}"],
              "path": ["realms", "{{realm}}", "protocol", "openid-connect", "token"]
            },
            "body": {
              "mode": "urlencoded",
              "urlencoded": [
                { "key": "grant_type", "value": "password" },
                { "key": "client_id", "value": "{{client_id}}" },
                { "key": "client_secret", "value": "{{client_secret}}" },
                { "key": "username", "value": "{{user2_username}}" },
                { "key": "password", "value": "{{user2_password}}" }
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "pm.test('status 200', () => pm.response.to.have.status(200));",
                  "const json = pm.response.json();",
                  "pm.collectionVariables.set('access_token_2', json.access_token);"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "2 - Tenants & Workspaces",
      "item": [
        {
          "name": "Create Tenant (as User 1 -> becomes owner)",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "auth": { "type": "bearer", "bearer": [{ "key": "token", "value": "{{access_token_1}}" }] },
            "url": { "raw": "{{base_url}}/tenants", "host": ["{{base_url}}"], "path": ["tenants"] },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Postman Test Tenant\",\n  \"slug\": \"postman-{{$randomUUID}}\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "pm.test('status 201', () => pm.response.to.have.status(201));",
                  "const json = pm.response.json();",
                  "pm.collectionVariables.set('tenant_id', json.id);"
                ]
              }
            }
          ]
        },
        {
          "name": "List My Tenants",
          "request": {
            "method": "GET",
            "header": [],
            "auth": { "type": "bearer", "bearer": [{ "key": "token", "value": "{{access_token_1}}" }] },
            "url": { "raw": "{{base_url}}/tenants", "host": ["{{base_url}}"], "path": ["tenants"] }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "pm.test('status 200', () => pm.response.to.have.status(200));",
                  "pm.test('includes created tenant', () => {",
                  "  const ids = pm.response.json().map(t => t.id);",
                  "  pm.expect(ids).to.include(pm.collectionVariables.get('tenant_id'));",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Create Workspace (tenant admin+)",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "auth": { "type": "bearer", "bearer": [{ "key": "token", "value": "{{access_token_1}}" }] },
            "url": {
              "raw": "{{base_url}}/tenants/{{tenant_id}}/workspaces",
              "host": ["{{base_url}}"],
              "path": ["tenants", "{{tenant_id}}", "workspaces"]
            },
            "body": { "mode": "raw", "raw": "{\n  \"name\": \"Postman Workspace\"\n}" }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "pm.test('status 201', () => pm.response.to.have.status(201));",
                  "const json = pm.response.json();",
                  "pm.collectionVariables.set('workspace_id', json.id);"
                ]
              }
            }
          ]
        },
        {
          "name": "List Workspaces",
          "request": {
            "method": "GET",
            "header": [],
            "auth": { "type": "bearer", "bearer": [{ "key": "token", "value": "{{access_token_1}}" }] },
            "url": {
              "raw": "{{base_url}}/tenants/{{tenant_id}}/workspaces",
              "host": ["{{base_url}}"],
              "path": ["tenants", "{{tenant_id}}", "workspaces"]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": { "exec": ["pm.test('status 200', () => pm.response.to.have.status(200));"] }
            }
          ]
        },
        {
          "name": "Add Tenant Member (user2)",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "auth": { "type": "bearer", "bearer": [{ "key": "token", "value": "{{access_token_1}}" }] },
            "url": {
              "raw": "{{base_url}}/tenants/{{tenant_id}}/members",
              "host": ["{{base_url}}"],
              "path": ["tenants", "{{tenant_id}}", "members"]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"user_id\": \"{{user2_internal_id}}\",\n  \"role\": \"member\"\n}"
            },
            "description": "REQUIRES `user2_internal_id` to be set first:\n1. Run 'Get Token - User 2' then any authenticated request as User 2 (e.g. duplicate 'List My Tenants' with access_token_2) — this JIT-provisions their `users` row.\n2. Look up their internal id: `psql -U mmrag -h localhost -d mmrag -c \"SELECT id FROM users WHERE email = 'testuser2@example.com';\"`\n3. Set the `user2_internal_id` collection variable to that UUID.\nThere is no GET /me endpoint yet to automate this step."
          },
          "event": [
            {
              "listen": "test",
              "script": { "exec": ["pm.test('status 201', () => pm.response.to.have.status(201));"] }
            }
          ]
        }
      ]
    },
    {
      "name": "3 - API Keys",
      "item": [
        {
          "name": "Create API Key",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "auth": { "type": "bearer", "bearer": [{ "key": "token", "value": "{{access_token_1}}" }] },
            "url": {
              "raw": "{{base_url}}/tenants/{{tenant_id}}/api-keys",
              "host": ["{{base_url}}"],
              "path": ["tenants", "{{tenant_id}}", "api-keys"]
            },
            "body": { "mode": "raw", "raw": "{\n  \"scopes\": [\"documents:read\"]\n}" }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "pm.test('status 201', () => pm.response.to.have.status(201));",
                  "const json = pm.response.json();",
                  "pm.test('key has mmrag_ prefix', () => pm.expect(json.key).to.match(/^mmrag_/));",
                  "pm.collectionVariables.set('api_key_id', json.id);",
                  "pm.collectionVariables.set('api_key_plaintext', json.key);"
                ]
              }
            }
          ]
        },
        {
          "name": "Use API Key (as bearer credential)",
          "request": {
            "method": "GET",
            "header": [],
            "auth": { "type": "bearer", "bearer": [{ "key": "token", "value": "{{api_key_plaintext}}" }] },
            "url": {
              "raw": "{{base_url}}/tenants/{{tenant_id}}/workspaces",
              "host": ["{{base_url}}"],
              "path": ["tenants", "{{tenant_id}}", "workspaces"]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": { "exec": ["pm.test('status 200', () => pm.response.to.have.status(200));"] }
            }
          ]
        },
        {
          "name": "List API Keys (never exposes hash/plaintext)",
          "request": {
            "method": "GET",
            "header": [],
            "auth": { "type": "bearer", "bearer": [{ "key": "token", "value": "{{access_token_1}}" }] },
            "url": {
              "raw": "{{base_url}}/tenants/{{tenant_id}}/api-keys",
              "host": ["{{base_url}}"],
              "path": ["tenants", "{{tenant_id}}", "api-keys"]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "exec": [
                  "pm.test('status 200', () => pm.response.to.have.status(200));",
                  "pm.test('no key material exposed', () => {",
                  "  for (const k of pm.response.json()) {",
                  "    pm.expect(k).to.not.have.property('key');",
                  "    pm.expect(k).to.not.have.property('key_hash');",
                  "  }",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "Revoke API Key",
          "request": {
            "method": "DELETE",
            "header": [],
            "auth": { "type": "bearer", "bearer": [{ "key": "token", "value": "{{access_token_1}}" }] },
            "url": {
              "raw": "{{base_url}}/tenants/{{tenant_id}}/api-keys/{{api_key_id}}",
              "host": ["{{base_url}}"],
              "path": ["tenants", "{{tenant_id}}", "api-keys", "{{api_key_id}}"]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": { "exec": ["pm.test('status 204', () => pm.response.to.have.status(204));"] }
            }
          ]
        },
        {
          "name": "Use Revoked API Key -> 401",
          "request": {
            "method": "GET",
            "header": [],
            "auth": { "type": "bearer", "bearer": [{ "key": "token", "value": "{{api_key_plaintext}}" }] },
            "url": {
              "raw": "{{base_url}}/tenants/{{tenant_id}}/workspaces",
              "host": ["{{base_url}}"],
              "path": ["tenants", "{{tenant_id}}", "workspaces"]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": { "exec": ["pm.test('status 401', () => pm.response.to.have.status(401));"] }
            }
          ]
        }
      ]
    },
    {
      "name": "4 - Negative & RBAC",
      "item": [
        {
          "name": "No token -> 401",
          "request": {
            "method": "GET",
            "header": [],
            "url": { "raw": "{{base_url}}/tenants", "host": ["{{base_url}}"], "path": ["tenants"] }
          },
          "event": [
            {
              "listen": "test",
              "script": { "exec": ["pm.test('status 401', () => pm.response.to.have.status(401));"] }
            }
          ]
        },
        {
          "name": "Garbage token -> 401",
          "request": {
            "method": "GET",
            "header": [],
            "auth": { "type": "bearer", "bearer": [{ "key": "token", "value": "not-a-real-jwt" }] },
            "url": { "raw": "{{base_url}}/tenants", "host": ["{{base_url}}"], "path": ["tenants"] }
          },
          "event": [
            {
              "listen": "test",
              "script": { "exec": ["pm.test('status 401', () => pm.response.to.have.status(401));"] }
            }
          ]
        },
        {
          "name": "Valid token, not a tenant member -> 404",
          "request": {
            "method": "GET",
            "header": [],
            "auth": { "type": "bearer", "bearer": [{ "key": "token", "value": "{{access_token_1}}" }] },
            "url": {
              "raw": "{{base_url}}/tenants/00000000-0000-0000-0000-000000000000/workspaces",
              "host": ["{{base_url}}"],
              "path": ["tenants", "00000000-0000-0000-0000-000000000000", "workspaces"]
            },
            "description": "Random tenant id nobody belongs to. Per 06-security-model.md §5, this must be 404, not 403 — a non-member can't distinguish 'doesn't exist' from 'exists but you can't see it'."
          },
          "event": [
            {
              "listen": "test",
              "script": { "exec": ["pm.test('status 404', () => pm.response.to.have.status(404));"] }
            }
          ]
        },
        {
          "name": "Member role, insufficient for admin action -> 403",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "auth": { "type": "bearer", "bearer": [{ "key": "token", "value": "{{access_token_2}}" }] },
            "url": {
              "raw": "{{base_url}}/tenants/{{tenant_id}}/workspaces",
              "host": ["{{base_url}}"],
              "path": ["tenants", "{{tenant_id}}", "workspaces"]
            },
            "body": { "mode": "raw", "raw": "{\n  \"name\": \"Should Fail\"\n}" },
            "description": "Requires 'Add Tenant Member (user2)' to have run first (user2 has role=member, but workspace creation needs admin+)."
          },
          "event": [
            {
              "listen": "test",
              "script": { "exec": ["pm.test('status 403', () => pm.response.to.have.status(403));"] }
            }
          ]
        },
        {
          "name": "Member role CAN list (member+ required) -> 200",
          "request": {
            "method": "GET",
            "header": [],
            "auth": { "type": "bearer", "bearer": [{ "key": "token", "value": "{{access_token_2}}" }] },
            "url": {
              "raw": "{{base_url}}/tenants/{{tenant_id}}/workspaces",
              "host": ["{{base_url}}"],
              "path": ["tenants", "{{tenant_id}}", "workspaces"]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": { "exec": ["pm.test('status 200', () => pm.response.to.have.status(200));"] }
            }
          ]
        }
      ]
    }
  ]
}
